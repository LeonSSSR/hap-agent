[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not live and not integration"
markers =
    live: requires AGENT_MODEL_API_KEY or live platform stack
    integration: requires full acceptance stack
